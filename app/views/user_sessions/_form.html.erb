<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login Page</title>
    <!-- Bootstrap CSS -->
    <%= stylesheet_link_tag "https://stackpath.bootstrapcdn.com/bootstrap/5.3.0/css/bootstrap.min.css" %>
</head>

<body class="bg-light bg-image-1">

<div class="container vh-100 d-flex justify-content-center align-items-center">
    <div class="card shadow-sm card-custom">
        <div class="card-body">
            <h3 class="card-title text-center mb-4">Login</h3>
            <%= form_with url: login_path, method: :post  do |f| %>
                <div class="mb-3">
                    <%= f.label :email, "Email address", class: "form-label" %>
                    <%= f.email_field :email, placeholder: "Enter your email", class: "form-control", required: true %>
                </div>
                <div class="mb-3">
                    <%= f.label :password, "Password", class: "form-label" %>
                    <%= f.password_field :password, placeholder: "Enter your password", class: "form-control", required: true %>
                </div>
                <div class="d-grid">
                    <%= f.submit "Login", class: "btn btn-primary" %>
                </div>
                <div class="text-center mt-3">
                    <a href="#" data-bs-toggle="modal" data-bs-target="#forgotPasswordModal">Forgot password?</a>
                </div>
            <% end %>
        </div>
    </div>
</div>

<!-- Modal -->
<div class="modal fade" id="forgotPasswordModal" tabindex="-1" aria-labelledby="forgotPasswordModalLabel" aria-hidden="true">
    <div class="modal-dialog">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title" id="forgotPasswordModalLabel">Forgot Password</h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
            </div>
            <div class="modal-body">
                This feature is premium. Please contact the developer team of  Mashed Bytes at rehannisar06@gmail.com to reset your password.
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Close</button>
            </div>
        </div>
    </div>
</div>

<!-- Bootstrap Bundle with Popper -->
<script src="https://stackpath.bootstrapcdn.com/bootstrap/5.3.0/js/bootstrap.bundle.min.js"></script>

</body>
</html>
