


<div class="container-fluid bg-image-1 vh-100 d-flex align-items-center">
  <div class="card mx-auto d-flex flex-column justify-content-between bg-white pt-4 pb-5">
    <h1 class="text-center fw-bold fs-1 mx-2 pb-4 mb-5 border-bottom">Dashboard</h1>

    <div class="d-flex justify-content-center mb-5">
      <div class="border-ts d-flex rounded-4 gap-3 px-3 py-2">
        <p class="fs-3 fw-bold m-0">Total sum:</p>
        <p class="fs-3 fw-bold m-0">
          <%= commas_separated_number(@total_sum) %>
        </p>
      </div>
    </div>

  <div class="d-flex align-items-center justify-content-center gap-3">
    <%= link_to "New Entry", new_entry_path, class: "btn new-entry-button" %>
    <%= link_to "Statement", entries_path, class: "btn statement-button"  %>
  </div>
  </div>
</div>
